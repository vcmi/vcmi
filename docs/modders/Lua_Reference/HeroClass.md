# HeroClass

Hero class definition (Knight, Cleric, Ranger, …). Static metadata shared by all heroes of that class; for a placed hero use HeroInstance.

### getJsonKey

Returns the JSON key (mod-scoped identifier) of this entity.

- returns `string`
