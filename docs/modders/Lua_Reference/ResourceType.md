# ResourceType

A resource definition (wood, ore, gold, ...). Obtained from Services:getResourceByName and passed to the calls that add or read a player's resources.

### getJsonKey

Returns the JSON key (mod-scoped identifier) of this entity.

- returns `string`
